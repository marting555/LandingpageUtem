---
const routeItems = [
  'Talleres de programación desde primer año',
  'Proyectos con empresas tecnológicas chilenas',
  'Certificaciones y talleres profesionales',
  'Apoyo académico y empleabilidad'
];
---

<section id="inicio" class="relative isolate overflow-hidden bg-gradient-to-br from-[#0047AB] via-[#0056D2] to-[#5B4FE5] text-white">
  <!-- Background image -->
  <div class="absolute inset-0">
    <img 
      src="/images/screenshot8.png" 
      alt="Tecnología digital" 
      class="w-full h-full object-cover opacity-30"
    />
    <div class="absolute inset-0 bg-gradient-to-br from-[#0047AB]/60 via-[#0056D2]/60 to-[#5B4FE5]/60"></div>
  </div>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 py-8">
    <div class="grid lg:grid-cols-[280px_1fr_280px] gap-5 items-stretch">
      <!-- Left column: Photo card -->
      <div class="lg:mt-0 flex -ml-12">
        <div class="rounded-3xl overflow-hidden bg-gradient-to-br from-indigo-900/30 to-purple-900/30 backdrop-blur-sm border border-white/20 shadow-2xl flex-1 relative">
          <img 
            src="/images/foto9.png" 
            alt="Estudiante UTEM" 
            class="w-full h-full object-cover bg-gradient-to-b from-blue-900/20 to-transparent"
            style="object-position: 20% center;"
          />
        </div>
      </div>

      <!-- Center content -->
      <div class="flex flex-col items-center justify-center text-center px-4">
        <div class="flex flex-wrap gap-2 mb-6 text-xs font-semibold uppercase tracking-wider justify-center">
          <span class="inline-flex items-center gap-2 rounded-full bg-white/15 backdrop-blur-sm px-3 py-1.5">
            <span class="w-2 h-2 rounded-full bg-green-400"></span>
            Acreditada 5 años
          </span>
          <span class="inline-flex items-center gap-2 rounded-full bg-white/15 backdrop-blur-sm px-3 py-1.5">
            <span class="w-2 h-2 rounded-full bg-cyan-400"></span>
            Jornada diurna · vespertina
          </span>
        </div>
        
        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold mb-4 leading-tight tracking-tight max-w-3xl">
          Crea soluciones digitales desde la UTEM
        </h1>
        
        <p class="text-sm sm:text-base mb-6 opacity-95 leading-relaxed max-w-2xl">
          Ingeniería en Informática UTEM combina formación técnica sólida, proyectos reales con la industria y un enfoque humano para liderar la transformación digital de Chile.
        </p>
        
        <div class="flex flex-wrap gap-4 justify-center mb-6">
          <a 
            href="#contacto" 
            class="inline-flex justify-center items-center px-8 py-3.5 rounded-lg bg-orange-500 text-white font-bold text-base shadow-lg hover:shadow-xl hover:bg-orange-600 transition-all"
          >
            Contáctanos
          </a>
          <a 
            href="#caracteristicas" 
            class="inline-flex justify-center items-center px-8 py-3.5 rounded-lg bg-orange-500 text-white font-bold text-base hover:bg-orange-600 transition-all shadow-lg"
          >
            Ver características
          </a>
        </div>
      </div>

      <!-- Right card: Ruta Formativa -->
      <div class="lg:mt-0">
        <div class="rounded-3xl overflow-hidden backdrop-blur-md border border-white/20 shadow-2xl relative h-full">
          <!-- Background image for Ruta Formativa -->
          <div class="absolute inset-0">
            <img 
              src="/images/screenshot8.png" 
              alt="Tecnología digital" 
              class="w-full h-full object-cover opacity-40"
            />
            <div class="absolute inset-0 bg-gradient-to-br from-indigo-900/70 to-purple-900/70"></div>
          </div>
          
          <div class="relative z-10 p-6 h-full flex flex-col">
            <div class="mb-6 text-center">
              <h3 class="text-base font-bold uppercase tracking-wider text-white mb-1">Ruta Formativa</h3>
              <div class="w-16 h-1 bg-purple-400 mx-auto rounded-full"></div>
            </div>
            
            <div class="flex-1 flex flex-col justify-center relative">
              {routeItems.map((item, index) => (
                <div class="relative" key={index}>
                  <div class="flex items-start gap-3 mb-4">
                    <div class="flex flex-col items-center">
                      <div class="flex-shrink-0 w-8 h-8 rounded-full bg-gradient-to-br from-purple-400 to-pink-400 flex items-center justify-center text-sm font-bold text-white shadow-lg">
                        {index + 1}
                      </div>
                      {index < routeItems.length - 1 && (
                        <div class="w-0.5 h-10 bg-gradient-to-b from-purple-400/80 to-purple-400/20 mt-2"></div>
                      )}
                    </div>
                    <div class="flex-1 pt-0.5">
                      <p class="text-sm font-semibold leading-snug text-white">{item}</p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
            
            <div class="mt-4 pt-4 border-t border-white/30 text-center">
              <p class="text-xs text-white/90 leading-relaxed font-medium">
                Formación integral con proyectos reales
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
